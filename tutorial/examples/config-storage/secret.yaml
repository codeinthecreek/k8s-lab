# See docs/tutorial/04-config-storage.md. stringData is a write-only
# convenience field - the apiserver base64-encodes it into .data on
# create/update; reading the object back always shows .data.
apiVersion: v1
kind: Secret
metadata:
  name: config-storage-demo-secret
type: Opaque
stringData:
  password: s3cr3t-demo-value
