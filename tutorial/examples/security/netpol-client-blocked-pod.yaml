# Deliberately does NOT match the NetworkPolicy's allowed ingress
# source label. See docs/tutorial/09-security.md.
apiVersion: v1
kind: Pod
metadata:
  name: security-demo-client-blocked
  labels:
    role: other
spec:
  containers:
  - name: busybox
    image: busybox:1.36
    command: ["sh", "-c", "sleep 3600"]
