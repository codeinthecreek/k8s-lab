# Pod Security Admission is enforced via plain namespace labels, not a
# separate object. See docs/tutorial/09-security.md.
apiVersion: v1
kind: Namespace
metadata:
  name: security-demo-restricted
  labels:
    pod-security.kubernetes.io/enforce: restricted
